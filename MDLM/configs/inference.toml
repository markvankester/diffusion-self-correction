# Default inference selector config for MDLM.
#
# Change task to switch between task-specific inference configs:
#   "arithmetic" -> MDLM/configs/inference_arithmetic.toml
#   "sudoku"     -> MDLM/configs/inference_sudoku.toml

task = "arithmetic"                          # Options: "arithmetic", "sudoku"
