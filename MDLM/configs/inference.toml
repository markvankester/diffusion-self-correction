# Default inference selector for `uv run MDLM/run_inference.py`.
#
# Change only this value to switch between task-specific inference configs:
#   "arithmetic" -> MDLM/configs/inference_arithmetic.toml
#   "sudoku"     -> MDLM/configs/inference_sudoku.toml

task = "sudoku"
